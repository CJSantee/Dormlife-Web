<%= form_with(model: [@college, @residence_hall, room], local: true) do |f| %>
    <div class="field">
        <%= f.label :room_number %>
        <%= f.text_field :room_number %>
    </div>
    <div class="actions">
        <%= f.submit %>
    </div>
<% end %>